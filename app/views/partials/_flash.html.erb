<% flash.each do |msg_type, msg| %>
	<% type = msg_type == 'notice' ? 'success' : 'danger' %>
	<%= content_tag :div, class: "alert alert-#{type}" do %>
		<%= msg %>
	  <button type="button" class="close" data-dismiss="alert">&times;</button>
	<% end %>
<% end %>